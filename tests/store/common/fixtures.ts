import { IApplicationState } from '../../../src/store/common/types';
import * as cameraStore from '../camera/fixtures';

export const FInitialState: IApplicationState = {
    camera: cameraStore.FInitialState,
};
